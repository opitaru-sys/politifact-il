# בדוק · Product Backlog

Living document. Tier order = priority. Check items off as they ship.
Last reconciled: 2026-05-24.

## Now / blocking

- [ ] **Re-run `apply-editorial-review.mts --apply` tomorrow (after 2026-05-28 06:00 UTC).** The 2026-05-27 sweep with new criteria #9 (retroactive) + #10 (characterization) hit Gemini's 10K/day quota partway through — only ~456 of 3478 claims were actually evaluated, the rest fail-opened (stayed approved, correct). Quota resets ~18h after the hit. The script is idempotent — claims already rejected by [עורך] are skipped, so re-running just finishes the remaining ~3000. **Now also includes criterion #12 (public interest gate)** — re-running picks up Goldknopf-pattern fluff across the corpus.

- [ ] **Three-bucket visibility model** — spec'd via back-and-forth with Codex on 2026-05-27. Replace the binary `editorApproved: true/false` with a three-state enum `visibility: "public" | "profile_only" | "rejected"`. Profile-only claims stay searchable on the politician page (in a "טענות פחות מרכזיות" expander) but never hit the homepage, leaderboard, digest, or topic feeds. Categorical AI assignment via the editor (no numeric scoring). Open design question: does profile_only count toward Wilson score? Strong lean toward NO (otherwise we're back to the dilution problem that motivated the 2026-05-27 fragmentation triage). Touches: Prisma schema, queries.ts, topic-stats.ts, cred-history.ts, digest synthesis, leaderboard, profile page, admin tooling. ~1-2 day project. Backfill 3,500+ existing live claims via AI sweep when it lands. Today's quick fix (editor criterion #12) is the bridge until then; it rejects-from-feed using the same logic the three-bucket model will use to set visibility.
- [ ] **Set Google Cloud budget alert** at ₪100-200/month. Without this, a runaway script could be expensive before you notice.

## High-impact next

- [ ] **Fragment-detection layer for the editorial agent.** The Barkat "הקרן שהקמתי" claim (2-word noun phrase, no predicate) passed all 4 layers because the verifier could verify the underlying fact (Barkat did found a fund). Quote-length audit found 57 approved claims at 1-3 words; many are legit (e.g. "20,000 ילדים הרוגים") but some are pure fragments ("הקרן שהקמתי", "החוק הזה עבר"). Word-count alone is too crude. Add to editorial-review.ts: "reject if the quote is a noun phrase without a complete clause — like 'the fund I founded' or 'the law that passed' — even if the underlying fact is verifiable." Then sweep existing approved claims for the pattern.
- [ ] **Neon serverless adapter** (`@prisma/adapter-neon` + `@neondatabase/serverless`) — HTTP instead of TCP, handles cold-starts gracefully. Eliminates the 500-on-first-request-after-suspend issue. ~1 hour.
- [ ] **More reliable cron than GitHub Actions free tier.** Schedules can be delayed 15-60 min during high system load. Pipeline is idempotent so missed ticks self-heal. Options: Vercel Cron Pro ($20/mo), Upstash QStash, $1/mo VPS with system cron. Lean QStash. ~30 min.

## Tier 1 · Data sources

- [ ] **Government press releases** — `gov.il` per-ministry feeds (PMO, Treasury, Defense, Education).
- [ ] **Politicians' X/Twitter feeds** — direct quotes, dated, attributable. Nitter is dead, rsshub.app started blocking, X API Basic is $200/mo. Telegram shipped instead (see "Recently shipped"). Re-evaluate X if Telegram yield is too low.

## Tier 2 · Data (higher effort)

- [ ] **YouTube interview transcripts** — Channel 12 morning shows, Patriotim, GLZ podcasts. Whisper for captions.
- [ ] **Existing fact-check coverage** — scrape Mako, Walla, Channel 12 fact-check sections; ingest with their verdicts.
- [ ] **Historical RSS backfill** — Israeli news sites have archive pages (e.g., `ynet.co.il/news/category/X?page=N`). Could write per-site scrapers to backfill weekend/holiday gaps. Brittle, ~6-10 hours dev each. Low ROI vs the Tier 1 items.

## Trust mechanics (next pre-launch milestone)

- [ ] **Human-reviewed badge** — `humanReviewed: boolean` field + distinct UI badge for claims an admin manually approved. Distinct from the silent AI second-pass approval that exists today. (User explicitly skipped hand-curated claims in 2026-05-24 review — keep on backlog as low-priority.)
- [ ] **Per-source quality tier** — tag each source as official/primary-press/secondary-press/social. Small chip next to source name.
- [ ] **Claim submission flow** — public form: "I heard X say Y on date Z" → moderation queue → admin reviews.
- [ ] **Methodology / coverage page** at `/methodology` — show sources scanned + last-fetched per source + claim yield. `/about` already covers the formula + the pipeline; this would be the operational view.

## Polish

- [ ] **Search by topic and claim text** — Postgres FTS on `quote + explanation + politician name`. Currently search is politician-only.
- [ ] **Per-claim OG image** (next/og route at `/claim/[id]/opengraph-image.tsx`). Politician avatar + verdict band + first 12 words of quote.
- [ ] **Subscribe to a politician by email** — Resend free tier + sub/unsub flow.
- [ ] **"Right of response" embed** — tokenised link for politicians' offices to add a one-paragraph response to a claim about them.
- [ ] **Dismissible disclaimer banner** (cookie-based).
- [ ] **"Report a missing politician" form**.
- [ ] **Accessibility** — high-contrast mode, non-color verdict signals (symbols ✓ / ~ / ✕ next to badges for colour-blind and printed).
- [ ] **Comparison view polish** — `/compare` exists but could use a deeper feature comparison. (Done 2026-05-17: dropdown is now filtered to politicians with claims, sorted by claim count desc, shows `name · N טענות`.)
- [ ] **Expose comment count near verdict** on the card.
- [ ] **Per-source health badge in admin** — flag a source with a red dot if last successful fetch was >24h ago.

## Recently shipped (do not re-litigate)

- **Insights reshape across the site (journalist voice)** (2026-05-26) — User feedback (insights manager background): the previous digest + topic-page insights were "bare-bones, face-level — not real insights." Three-front reshape:
  1. **Digest pipeline reborn**: `src/lib/digest-analysis.ts` computes patterns (week-over-week verdict shifts, persistence rate, volume vs accuracy, topic distribution, first-time politicians, source mix); `src/lib/digest-synthesis.ts` sends the blob to Gemini with a detailed Hebrew prompt demanding senior-journalist voice (Haaretz/Calcalist register) with explicit ban list (em-dashes, "ראוי לציין", "מצד שני", "מעניין ש", etc.). Returns 4-9 insight paragraphs as JSON. Drops the "biggest false claim" section type (subjective + uninteresting). Falls back to deterministic minimal draft on AI failure. First generated draft produced 6 real insights including "שיח הביטחון כר פורה לאי-דיוקים", "מובילי נפח, חסרי דיוק", "היצמדות לחוסר אמינות (89%)".
  2. **Topic page insights band rewrite**: replaced the 2-cell summary with 3-6 multi-paragraph journalist-voice analyses (site comparison, dominant voice, verdict-shape, discrepancy positive/negative, spread). Deterministic templates (no per-render AI call), each paragraph only fires when its pattern is genuinely interesting.
  3. **Profile TopicBreakdown insight header**: added a leading paragraph above the topic rows explaining the spread between this politician's strongest and weakest topics. Fires only when spread ≥ 15 points; story-shaped when ≥ 30.
- **User-feedback round: discoverability + insights + share + cron** (2026-05-26) — Four targeted fixes after the user pointed out gaps:
  1. **Topics discoverability** — new `/topics` index listing all 13 canonical topics with claim counts + per-topic raw % (sorted by most-discussed first). Added `נושאים` to global header nav. `<ClaimCard>` topic chip now links to `/topic/[slug]` for canonical topics (was `/?topic=X` filter). The previously-orphaned topic pages are now genuinely findable.
  2. **Insights band on /topic/[slug]** — new card at the top: aggregate raw % on this topic vs site-wide raw % (+ delta in nekudot), plus "biggest discrepancy" rows showing the politician whose topic score is most different from their overall score. Self-hides components if data is too thin.
  3. **Admin preview** — `/admin/digest` now renders the actual `<DigestRenderer>` output inline below the edit form for each digest. Editor sees exactly what readers will see, no need to publish first.
  4. **Share button on hero** — `<LiarOfTheWeek>` primary card was wrapped in `<a>`, blocking nested interactive share buttons. Restructured to `<div>` outer + explicit footer with "read more" `<Link>` + `<ShareButtons>` (uses existing `shareTextForHero`).
  5. **Friday cron** — `.github/workflows/weekly-digest.yml` runs `generate-weekly-digest.mts --apply` Friday at 03/04 UTC (DST safety pattern matching daily-ingest). Auto-generates draft only — admin still has to approve to publish. The draft IS the weekly reminder.
- **Weekly digest at /digest with admin approval** (2026-05-26) — Content product giving readers a habit-builder reason to return. Pipeline: `scripts/generate-weekly-digest.mts` runs (manually for now, schedule on Friday later) and produces a structured draft Digest row with 4 auto-generated sections: (1) headline stats ("X claims this week"), (2) movers (top 3 ↑ + top 3 ↓ from cred-history), (3) the week's biggest false claim (highest-confidence שקר), (4) most-debated canonical topic. Admin reviews + edits at `/admin/digest?key=...` — single textarea per field (title, intro, sections-as-JSON) plus publish/unpublish/delete actions. Public renders at `/digest` (latest published) and `/digest/[YYYY-MM-DD]` (specific past issue) via shared `<DigestRenderer>`. New Prisma model `Digest` with unique constraint on `weekOf` (Friday date). Home page links via small "השבוע באמינות ←" chip in the masthead eyebrow. **To publish the first issue**: run generator → log into `/admin/digest?key=...` → tweak → click "פרסם". **Iteration backlog**: per-section structured UI (right now it's raw JSON), Friday cron, /digest archive index page, header nav link.
- **Topic landing pages at /topic/[slug]** (2026-05-26) — Each canonical topic gets its own URL (`/topic/security`, `/topic/economy`, `/topic/justice`, ...). Page shows top + bottom credibility on the topic + recent claims feed + other-topics nav. English slugs (better SEO than URL-encoded Hebrew). 13 canonical topics from `NORMALIZATIONS` in `src/lib/topics.ts`; new helpers `slugToTopicLabel`, `topicLabelToSlug`, `listCanonicalTopics`, `rawTopicMatchesSlug`. New `src/lib/topic-stats.ts` for the page's two queries; loads window-scoped claims and filters in-memory by regex (Prisma `contains` can't express the canonical patterns cleanly). TopicBreakdown rows on profile pages now link to `/topic/[slug]` instead of the filtered home feed for canonical topics. `generateStaticParams` exposes all slugs for SEO crawl. 30-day-window coverage right now: security/economy/justice/foreign-policy/education/society all populate well; hostages/housing/fallen sparse.
- **Trend band in LiarOfTheWeek hero card** (2026-05-26) — User flagged wasted whitespace on the home hero card. Added 30-day trend band at the bottom of the primary "במקום הראשון" card: inline RTL sparkline (~100×28px, custom SVG) + delta text ("↑ +4.2 נקודות") + one-line context ("שיפור באמינות בחודש האחרון"). Bottom "במקום האחרון" small card gets a compact delta chip next to the score. Both self-hide when there aren't enough usable snapshot points (min 3 weeks with ≥5 claims each). `LiarOfTheWeek` became `async` to fetch `getPoliticianTimeline` for top + bottom politicians; queries run in parallel so home-page latency unchanged. Same flat-threshold logic (±2 points = grey) as the full timeline chart so visual semantics match.
- **Topic breakdown card on politician profile** (2026-05-26) — New `<TopicBreakdown>` card on `/politician/[id]` between the timeline chart and the Knesset activity card. Groups the politician's window-filtered claims by normalized topic (uses new `normalizeTopic()` helper in `src/lib/topics.ts` — same regex library as `topicDisplayLabel` but no truncation, so semantically similar topics like "מדיניות הביטחון בגבול" + "מבצע צבאי" both bucket into ביטחון). Per topic: Wilson 95% lower bound + raw % + sample count + 3-color verdict bar. Threshold: min 5 claims per topic, min 2 topics; card self-hides otherwise. Row click → `/?politician=X&topic=Y` to read the underlying claims. At 30-day window, ~5/117 politicians qualify (thin); the card surfaces for more as users widen the window selector. Pure in-memory work on already-loaded claims, no extra DB query.
- **Homepage refactor: cut from 7 sections to 5** (2026-05-26) — Homepage was accreting cards with every new feature (LotW + Leaderboard + Movers + Parties + InHeadlines + Discovery card + 4-line legend = 7 sections before the feed, ~5000px scroll). Cut: (a) PartiesPreview removed from home — already at `/parties`, accessible via the always-visible header nav. (b) Big "חפשו פוליטיקאי" discovery card removed; replaced with a slim one-line SearchBar (new `compact` prop) co-located with InHeadlinesStrip as a one-click escape hatch. (c) 4-line legend compressed to one line with a "איך מחושב? ←" link to `/about`. PartiesPreview.tsx file kept (orphaned) in case we want to re-introduce it. **Governing principle for new features**: new insight pages get their own URLs (`/topic/[slug]`, `/digest`, etc.) — the homepage stays curated, depth lives one click in.
- **Credibility timeline + biggest movers (the "so what" layer)** (2026-05-26) — New `CredibilitySnapshot` Prisma model stores per-politician Wilson scores over a 30-day rolling window, snapshotted nightly (wired into `scripts/daily.mts`) and backfilled 12 months via `scripts/backfill-cred-snapshots.mts` (682 historical rows across 53 weekly samples). New `<CredibilityTimeline>` SVG chart on `/politician/[id]` with 3/6/12-month selector, line colored by net direction (green up / red down / grey flat ≤±2pts), gaps for low-sample weeks. New `<BiggestMovers>` two-column card on home page (gainers↑ vs. losers↓) using 7-day delta with `minSample=15` in both anchor windows; card self-hides if `gainers + losers < 4`. **Note**: at minSample=15 the dataset currently yields only 3 movers total (card hidden). Lower `minSample` in `src/app/page.tsx` once we want it visible, or wait for the dataset to grow. Reads via `src/lib/cred-history.ts`. Custom SVG (no chart library) — RTL-aware (oldest on right, newest on left).
- **Current-officeholders block + stale-officeholder sweep** (2026-05-26) — Fixed the visible "Trump isn't the US president" bug on a live Vaturi claim. Even with Google Search grounding enabled, Gemini sometimes overrides search results with its strong pre-training prior (Biden/Gallant/Halevi). New `src/lib/officeholders.ts` exports `currentOfficeholdersBlock()` injected into BOTH the fact-check preamble (`dateContextPreamble`) and the verifier preamble. New verifier criterion #12 rejects any explanation that asserts a stale officeholder is current or that the actual current officeholder isn't. `scripts/sweep-stale-officeholders.mts` swept 2909 approved claims and caught 5 cases of the bug (Trump/Biden, Katz/Gallant, Zamir/Halevi). All hidden with correction notes. See memory `ai_temporal_confusion.md` (now 5 layers, not 3). **Maintenance: when an officeholder changes, update `officeholders.ts` — both prompts pick it up automatically.**
- **"מי בכותרות" strip + prominent search section on home** (2026-05-26) — `InHeadlinesStrip` component below parties preview shows top 8 politicians by RAW claim count in the active window (pure volume sort, no editorial bias). Solves "first-time visitors don't see Bibi/Lapid/Smotrich because Wilson penalizes their sample, leaderboard fills with less-famous MKs". Each card has credibility score chip + click → profile. Search bar moved up into a bordered "חפשו פוליטיקאי" discovery section right after the strip. Share buttons added to header of LeaderboardPreview, PartiesPreview, InHeadlinesStrip via new `shareTextForRanking`. See memory `homepage_discoverability.md`.
- **Editor agent (4th-pass)** (2026-05-26) — `src/lib/editorial-review.ts` runs after the verifier when a claim is approved. Single Gemini call (~$0.001/claim, no grounding) asking "is this worth publishing as a fact-check, or routine PR / personal acknowledgment?" Conservative dial, ~3.5-7% rejection rate. Backfill on 2026-05-26 reviewed all 2,875 historical approved claims → 102 un-approved (logged to /corrections with `[עורך]` prefix in `verifierNotes`). Skip via `BADAK_DISABLE_EDITOR=1` during big drains. `scripts/apply-editorial-review.mts` for backfill.
- **credibilityScore is the headline number now (was only used for sort)** (2026-05-26) — User feedback: showing raw `truthPercentage` while sorting by Wilson `credibilityScore` was confusing ("Tibi at 84% ranked above Katz at 100%"). All cards (hero, leaderboard preview, /leaderboard, /parties, /politician/[id], /compare, PartiesPreview, InHeadlinesStrip) now display `credibilityScore` as the big number. Raw % stays visible as a sub-line. `scoreColor` thresholds apply to `credibilityScore`. See memory `ranking_wilson_score.md`.
- **Option A — sweeping characterizations now extracted** (2026-05-26) — Extraction prompt softened: claims like "כל ערביי ישראל תומכים בטרור" or "השופטים בבג״ץ נטויים שמאלה" are now extracted when they have implicit factual content. The verifier handles classification (usually "false" or "half-true" with evidence). Still rejects pure-emotional opinion, slogans, and direct personal insults. Fixes the "loud politicians who say obviously-not-true things never get checked because their claims are rhetorical" gap.
- **Eulogy/ceremonial/news-narrative pattern expansion** (2026-05-26) — Added `eulogy-memorial` issue code (ז״ל with `(?<!ח)` lookbehind for חז״ל, נפל בקרב, הותיר אחריו, הלוויה, blessings, religious-personal) + `ceremonial` code (אני מודה, אבקש להודיע, אני שמח לבשר). Added 30 more news-narrative verbs (עיכב, תקף, גינה, דרש, etc.) after a Netanyahu "עיכב את פרסום הדוח" claim was approved. **Critical fix**: `FIRST_PERSON_RE` used `\b` which is ASCII-only — silently never matched Hebrew. Replaced with `hasFirstPersonMarker()` using existing `hebrewWordMatch()` helper. Don't use `\b` on Hebrew. See `quality_gate_triple_defense.md`.
- **/compare uses credibilityScore + intro rewrite** (2026-05-26) — Compare page was still showing raw `truthPct` — inconsistent with leaderboard. Imports `wilsonLowerBound` from queries.ts, computes inline from window-filtered claims, displays as headline with raw % as sub-line. Tooltip explains the sample adjustment.
- **/about page methodology rewrite** (2026-05-26) — Updated "איך זה עובד" to reflect the 5-step pipeline (filter → extract → fact-check → verifier → editor → publish). Mentions 23 Telegram channels + cron cadence. New "ציון אמינות — איך מחשבים?" section explains Wilson in plain Hebrew. Added bullet acknowledging sweeping characterizations get extracted under Option A. Fixed typo "לעדר העובדות" → "לעולם העובדות".
- **Knesset queue drain** (2026-05-26) — Drained 108 pending Knesset transcripts with grounding disabled (standard low-cost Knesset lane). Produced 225 new claims; 129 editor-approved (57%), 94 hidden by verifier (typical for ungrounded transcripts), 2 hidden by editor. Cost ~$0.30. Queue at 0 after.
- **Project-level `bypassPermissions`** (2026-05-26) — `badak/.claude/settings.json` with `defaultMode: "bypassPermissions"` as belt-and-suspenders alongside the global file. `.claude/` is gitignored so this is local-dev only. Active session mode (Shift+Tab toggle) still overrides the file default; check status bar to verify.
- **Telegram coverage expanded to 23 channels** (2026-05-17) — Probed every politician in NAME_TO_ID with plausible handle variants via a discovery script; found 15 additional active channels on top of the original 8. New: Lapid, Smotrich, Eisenkot, Sofer, Elkin, Haskel, Kisch, Karhi, Chikli, Distel, Golan, Kallner, Silman, Barkat, Ohana. Still missing across the spectrum: all Haredi parties, Liberman, Arab parties, Labor, Gantz, Bennett — these constituencies don't keep public Telegram presence. Backfill drain on 2026-05-17 processed ~200 posts → 168 claims → 133 editor-approved (79% pass rate), spend ~$9. Coverage bias toward coalition documented in `telegram-sources.ts`.
- **Telegram ingest** (2026-05-17) — Scrapes `t.me/s/<channel>` HTML (no auth, no third-party service). Each post wrapped with `פוסט בטלגרם של <name>:` attribution so the existing extraction prompt's first-person / attribution heuristics don't false-reject. Telegram source names are in `FRESH_SOURCE_NAMES` so the fresh-news lane processes them alongside RSS. Implementation: `src/lib/telegram-sources.ts` + `fetchTelegramChannel()` in `ingest.ts`.
- **OG image RTL fix** (2026-05-17) — The earlier `rtl()` workaround used split-by-word reversal that misplaced punctuation (question mark ended up mid-sentence) and inverted word order so a Hebrew reader saw gibberish. Replaced with full-codepoint reverse (`Array.from(s).reverse().join("")`), confirmed visually: brand wordmark now anchored right, headline reads correctly R→L, "?" lands at the visual end. Block alignment fixed with `alignItems: flex-end` on the headline column.
- **Parties preview on home** (2026-05-17) — `PartiesPreview` component renders below the hero+leaderboard duo with the top-5 parties by truthPercentage and 3-color verdict bars. Driven by the same global `?window=` selector. `getPartyStats()` now accepts a `windowDays` argument.
- **Compare dropdown filtered** (2026-05-17) — `/compare` selectors only show politicians with ≥1 published claim (via new `getPoliticiansWithClaimsLite()`), sorted by claim count desc. Search remains unfiltered for discovery; compare is for analysis.
- `/corrections` public log + `correctionNote` / `correctedAt` schema. 234 historical corrections backfilled. Sweeps + admin editor write the fields atomically going forward. See memory `corrections_log.md`.
- Knesset activity dashboard — `KnessetActivity` model, daily OData ingest, `KnessetActivityCard` on `/politician/[id]`, attendance column on `/leaderboard`, per-party avg on `/parties`, `נוכחות` line in hero + leaderboard preview, home-page legend. See memory `knesset_activity.md`.
- Quality-gate triple defense — `src/lib/claim-quality.ts` shared module powering extraction-time block (`processArticle`) + verifier criteria 8/9 + retroactive sweep. See memory `quality_gate_triple_defense.md`.
- ADMIN_SECRET rotated (user did this 2026-05-24).
- `bduk.co.il` DNS published 2026-05-24; site primary domain switched off `politifact-il.vercel.app`. `SITE_URL` GitHub secret removed.
- PostHog Web Analytics (EU region), no cookies, `person_profiles: "identified_only"`.
- News-narrative + hyperbolic-insult rejection at extraction prompt, verifier criteria 8+9, and retroactive sweep (16 claims un-approved).
- RSS ingest URL blocklist drops sports paths + Ynet English/Russian editions before they hit the DB.
- Fresh-process cron staggered to `:15` so it runs 15min after RSS ingest, not in parallel.
- Suspense-streamed claim feed with FeedSkeleton + "טען עוד" pagination via `/api/claims`.
- Gemini 2.5 Flash + Google Search grounding pipeline (replaced Anthropic Sonnet + web_search — see `KNOWN_COSTS.md`).
- Three-lane processing in `scripts/daily.mts`: fresh RSS first, then Knesset ingest, then RSS backlog, then Knesset backlog (grounding off by default for Knesset).
- GitHub Actions cron: `rss-ingest` (30min), `fresh-process` (2h), `daily-ingest` (06+07 UTC).
- `/api/process?mode=fresh|rss-backlog|knesset|all` endpoint with `Authorization: Bearer ADMIN_SECRET`.
- Strict extraction prompt (rejects rhetoric, slogans, procedural Knesset content).
- Verifier criteria 7-tier with "verdict reflects content, not attribution" check.
- Three-layer claim-date threading so "we are now in March" Knesset quotes aren't judged against today's May date.
- Unified `?window=` selector (1/7/30/60/90 days, default 30; no "all") on home/leaderboard/politician — controls hero, leaderboard preview, AND the recent-claims feed in one filter.
- Tie-breaking by total-claims at both ends of the leaderboard (more claims wins ties).
- Claim detail page rewritten as an evidence file with ClaimReview JSON-LD.
- Admin: claim editor (`/admin/claims`), reports with dismiss + edit links, status with schedule + queue-age + daily-snapshot card.
- `DailySnapshot` Prisma model written at end of each daily cron run.
- Ben Gurion headstand spinner replaces the previous text-only loading state.
- All 29 politicians with claims have images (mix of local + Wikipedia-fetched).
- 14 RSS feeds (real-browser UA for Israel Hayom + Calcalist 403 fix).
- Public queries filter `editorApproved: true` everywhere; sweep scripts in `/scripts` for cleanup.
- Manual cleanup last run: ~93 Knesset roll-calls rejected; ~674 unverifiable-explanation claims unapproved (214 of which were publicly visible); 16 dedup duplicates removed.
- Mobile fixes: report button is bottom-sheet, AI disclaimer is full-text on every viewport, beta strip is compact on small screens.

## Memory (still true, don't re-litigate)

- Stats window selector is `1/7/30/60/90` days, default 30; "all-time" was deliberately removed.
- `MIN_CLAIMS_FOR_RANKING = 3`, `MIN_CLAIMS_FOR_HERO = 3`.
- No em-dashes in UI copy.
- Civic Press: cream, ink, press-red accent; no emojis in chrome; wordmark logo with niqqud (בָּדוּק).
- Satori (next/og) doesn't apply Hebrew bidi; pre-reverse strings in OG image source.
- Comment + report APIs rate-limited via Upstash (5/min, 3/min).
- Anthropic SDK was uninstalled. Don't re-introduce it; the migration to Gemini is final.
- The `BADAK_DISABLE_GROUNDING=1` env switch is for bulk drains only; the daily cron's fresh-news lane MUST keep grounding ON.
